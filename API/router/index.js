const productRouter = require('./routerProducts');
const categoriesRouter = require('./routerCategories');
const productCatRouter = require('./routerProductCat')

module.exports = {
  productRouter,
  categoriesRouter,
  productCatRouter
}
